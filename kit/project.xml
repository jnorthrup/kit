<project>

    <!-- attention, jnlp plugin (and uberjar)  is deprecated and maven-1.0.2 is all you got! -->
    <name>kit</name>
    <currentVersion>0.2.0</currentVersion>
    <groupId>kit</groupId>
    <organization>
        <name>vsiwest</name>
        <url>http://vsiwest.com</url>
    </organization>
    <build>
        <sourceDirectory>src</sourceDirectory>
        <unitTestSourceDirectory>test</unitTestSourceDirectory>
        <!--resources>
         <resource>
           <targetPath/>
           <filtering/>
           <directory/>
           <includes/>
           <excludes/>
         </resource>
       </resources -->
    </build>
    <dependencies>
        <dependency>
            <groupId>postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <version>8.2dev-503.jdbc3</version>
            <properties>
                <jnlp.jar>true</jnlp.jar>
            </properties>
        <type/></dependency>
        <dependency>
            <groupId>xstream</groupId>
            <artifactId>xstream</artifactId>
            <version>1.1.3</version>
            <properties>
                <jnlp.jar>true</jnlp.jar>
            </properties>
        </dependency>
        <dependency>
            <groupId>QTJava</groupId>
            <artifactId>QTJava</artifactId>
            <version>6.4.k</version>
            <properties>
                <jnlp.jar>true</jnlp.jar>
            </properties>
        </dependency>
        <dependency>
            <groupId>dom4j</groupId>
            <artifactId>dom4j</artifactId>
            <version>1.6.1</version>
            <url>http://www.dom4j.org</url>
            <properties>
                <jnlp.jar>true</jnlp.jar>
            </properties>
        </dependency>
        <dependency>
            <groupId>tools</groupId>
            <artifactId>javaws</artifactId>
            <version>1.6.0</version>
            <properties>
                <jnlp.jar>false</jnlp.jar>
            </properties>
        </dependency>
        <dependency>
            <groupId>rhino</groupId>
            <artifactId>js</artifactId>
            <version>k</version>
            <properties>
                <jnlp.jar>true</jnlp.jar>
            </properties>
        </dependency>

        <dependency>
            <groupId>poi</groupId>
            <artifactId>poi</artifactId>
            <version>2.5.1</version>
            <properties>
                <jnlp.jar>true</jnlp.jar>
            </properties></dependency>
       
        <!-- core dependencies -->
        <!-- dependencies used by optional features -->
        <!-- only used for command line tools -->
        <!-- only used for Ant tasks & scripting tools -->
        <!-- only used for testing helper classes -->
        <!-- only used for testing/debugging-->
        <!-- only used for BSF adap
        ter-->
        <!-- only used for JMX utilities -->
        <!-- only used for GroovyMock tools -->
        <!-- only used for EJB browser -->
        <!-- only used for SQL library -->

        <!-- only used for servlet / gsp -->
        <dependency>
            <groupId>picocontainer</groupId>
            <artifactId>picocontainer</artifactId>
            <version>1.4-k</version>
            <properties>
                <jnlp.jar>true</jnlp.jar>
            </properties>
        </dependency>
        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
            <version>3.8.1</version>
            <properties>
                <jnlp.jar>false</jnlp.jar>
            </properties>
        </dependency>
        <!-- only used for the Wiki Ant tasks -->
        <!-- only used for the HTML to Wiki script -->
        <!-- only used for the JavaDoc generator script -->
        <!-- only used in the samples (BlogLines client) -->
        <!-- used to dump out the AST -->
        <!-- used by xstream 
        <dependency>
            <groupId>xpp3</groupId>
            <artifactId>xpp3_xpath</artifactId>
            <version>1.1.3.4.O</version>
            <properties>
                <jnlp.jar>true</jnlp.jar>
            </properties>
        </dependency>-->
        <dependency>
            <groupId>jaxen</groupId>
            <artifactId>jaxen</artifactId>
            <version>1.1-beta-9</version>
            <properties>
                <jnlp.jar>true</jnlp.jar>
            </properties>
        </dependency>
        <!-- used to build embeddable jar -->
    </dependencies>
    <profiles>
        <profile>
            <id>default-tools.jar</id>
            <activation>
                <property>
                    <name>java.vendor</name>
                    <value>Sun Microsystems Inc.</value>
                </property>
            </activation>
            <dependencies>
                <dependency>
                    <groupId>com.sun</groupId>
                    <artifactId>tools</artifactId>
                    <version>1.4.2</version>
                    <scope>system</scope>
                    <systemPath>${java.home}/../lib/tools.jar</systemPath>
                </dependency>
            </dependencies>
        </profile>
    </profiles>
    <artifactId>kit</artifactId>
    <url>http://origin-testcustomer.vsiwest.com/kit/</url>
    <inceptionYear>2006</inceptionYear>
    <package>gui</package>
<shortDescription>vsiwest IT Tools</shortDescription><description>contains XMI code generator for openlaszlo, mp4,
    qt-java, tools, tunnels itself as needed, and last but hardly least, the database43 metadata aware browser and
    reports.
</description><mailingLists><mailingList><name/></mailingList></mailingLists></project>
